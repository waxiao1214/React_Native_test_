import { StyleSheet } from 'react-native';

const PartyRatingStyle = StyleSheet.create({
  root: {
    backgroundColor: '#25233D',
    height: "100%",
    paddingLeft: 20,
    paddingRight: 20,
  }
})

export default PartyRatingStyle;